'use strict';

var _toUsernameAndEmail = require('./toUsernameAndEmail');

var _toUsernameAndEmail2 = _interopRequireDefault(_toUsernameAndEmail);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('toUsernameAndEmail', function () {
  it('username', function () {
    expect((0, _toUsernameAndEmail2.default)({ user: 'UserA' })).toEqual({
      username: 'UserA',
      email: null
    });
  });
  it('email', function () {
    expect((0, _toUsernameAndEmail2.default)({ user: 'UserA@users.com' })).toEqual({
      username: null,
      email: 'UserA@users.com'
    });
  });
  it('username and email', function () {
    expect((0, _toUsernameAndEmail2.default)({
      user: null, username: 'UserA', email: 'UserA@users.com'
    })).toEqual({
      username: 'UserA',
      email: 'UserA@users.com'
    });
  });
});